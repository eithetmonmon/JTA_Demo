
create table `spring_student`.`student`(
    id int,
    name varchar (32),
    subject varchar (32)
);
create table `spring_address`.`address`(
    id int,
    street_name varchar (32),
    postal_code varchar (32)
);